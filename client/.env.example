# Default: deployed Render API (used for local dev and production builds)
VITE_API_URL=https://connectify-api-34ww.onrender.com/api

# To test against a local backend instead, create .env.local with:
# VITE_API_URL=http://localhost:5000/api
